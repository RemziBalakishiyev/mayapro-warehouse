namespace MayaPro.WarehouseApi.Modules.Expenses.Application.UseCases.CreateExpenseType;

/// <summary>Input for adding a managed expense type.</summary>
public sealed record CreateExpenseTypeCommand(string Name);
